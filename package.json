{
  "name": "template-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint": "eslint",
    "commit": "./scripts/quick-commit.sh",
    "git:quick": "git add -A && git status && echo 'Ready to commit!' && echo 'Run: git commit -m \"your message\" && git push'",
    "git:status": "git status --short --branch"
  },
  "dependencies": {
    "eslint": "^9.35.0"
  }
}
